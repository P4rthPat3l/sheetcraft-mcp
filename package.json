{
  "name": "sheetcraft-mcp",
  "version": "1.0.3",
  "type": "module",
  "description": "SheetCraft MCP — reliable, token-efficient Google Sheets MCP server + CLI for AI agents. 32 tools, teaching errors, capped reads, dual auth (OAuth + service account).",
  "license": "MIT",
  "author": "Parth Patel",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/P4rthPat3l/sheetcraft-mcp.git"
  },
  "homepage": "https://github.com/P4rthPat3l/sheetcraft-mcp#readme",
  "bugs": "https://github.com/P4rthPat3l/sheetcraft-mcp/issues",
  "keywords": [
    "mcp",
    "google-sheets",
    "sheets",
    "model-context-protocol",
    "mcp-server",
    "google-workspace",
    "spreadsheet",
    "cli"
  ],
  "bin": {
    "sheetcraft-mcp": "dist/wrappers/mcp.js",
    "sheets-mcp": "dist/wrappers/mcp.js",
    "sheets": "dist/wrappers/cli.js"
  },
  "files": [
    "dist",
    "!dist/test",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "prepack": "npm run check && npm run build",
    "test": "npm run build && node --test --test-reporter=spec \"dist/test/*.test.js\" 2>&1 | tail -12",
    "test:full": "npm run build && node --test --test-reporter=spec \"dist/test/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "googleapis": "^144.0.0",
    "google-auth-library": "^9.15.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
